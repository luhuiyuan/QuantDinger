## 1. 依赖与基础结构

- [x] 1.1 从 `/home/quantadinger/easy_tdx` 的 `1.20.4` / `3b7ce97` 基线构建本地 wheel，并记录 wheel 文件名、SHA256、许可证与 Python/pandas 兼容性
- [x] 1.2 让开发期后端打包流程安装该本地 wheel，保持 Docker 依赖层先于应用源码；不发布公共包、不复制 sibling 源码且不使用 editable install
- [x] 1.3 建立 A 股历史领域模块边界与内部 DTO，使仓储、供应商、同步、质量和回测层不直接泄露 easy_tdx 类型
- [x] 1.4 增加市场历史功能开关、同步批大小、请求节流、重试次数和磁盘软硬阈值配置及安全默认值
- [x] 1.5 为新增配置补充 `env.example` 文档，确保不包含真实凭据或部署秘密

## 2. 数据库迁移与仓储

- [x] 2.1 设计并添加 A 股原始日线、公司行动和复权因子表的日期迁移、主键、约束及查询索引
- [x] 2.2 添加同步批次、单标的检查点、供应商健康、质量问题和覆盖摘要表及索引
- [x] 2.3 将新增数据库对象以幂等形式同步到 `backend_api_python/migrations/init.sql`
- [x] 2.4 实现规范 `CNStock:<code>.<exchange>` 标识解析与拒绝不支持证券类型的单元测试
- [x] 2.5 实现原始日线幂等 upsert、历史修订/version 记录和按日期范围查询仓储
- [x] 2.6 实现公司行动与复权因子的幂等仓储、版本查询和失效标记
- [x] 2.7 实现同步批次、单标的检查点、质量问题及覆盖摘要仓储
- [x] 2.8 验证日期迁移可在现有数据库向前应用，并记录关闭功能开关的非破坏性回滚步骤

## 3. TDX 供应商与同步引擎

- [x] 3.1 实现 easy_tdx 适配器的连接生命周期、字段转换、超时和内部错误分类
- [x] 3.2 实现候选节点探测、延迟/失败健康评分、退避和受控节点切换
- [x] 3.3 实现日线分页、目标区间终止条件、有限重试与请求节流
- [x] 3.4 使用 easy_tdx 的完整历史接口采集公司行动，并对计算复权所需字段做完整性校验
- [x] 3.5 实现按标的串行、小批提交、检查点续传的同步服务
- [x] 3.6 使用 PostgreSQL advisory lock 防止重叠同步域被多个 worker 并发执行
- [x] 3.7 在任务创建和每个安全提交边界实现磁盘软硬阈值检查与暂停状态
- [x] 3.8 添加 Celery 维护任务，支持定向同步、失败标的重试和串行日常增量同步
- [x] 3.9 为分页中断、节点切换、重复投递、worker 恢复和磁盘暂停编写聚焦测试

## 4. 质量、覆盖与复权

- [x] 4.1 基于 XSHG 交易日历、证券有效期和停牌状态实现预期交易日计算
- [x] 4.2 实现中间缺口、OHLC 关系、非正价格、负成交量/成交额、重复日和日历外数据检查
- [x] 4.3 实现异常跳变与公司行动解释校验，并区分提示级和阻断级质量问题
- [x] 4.4 实现覆盖摘要刷新及执行前逐标的缺口/阻断问题精确校验
- [x] 4.5 实现前复权与后复权因子算法、锚点、内容标识和算法版本
- [x] 4.6 实现 `raw`、`forward`、`backward` 查询模式，保证交易日集合一致并返回来源版本
- [x] 4.7 为停牌、上市前日期、中间缺口、事件修订、因子缺口及确定性重算建立基准测试样本
- [x] 4.8 接入上交所/深交所结构化除权参考价，保存来源 URL、响应哈希、验证时间和官方参考价
- [x] 4.9 在因子生成前核对官方参考价；普通事件保留精确公式，差异化纯现金事件使用审计比例，复杂不一致事件失败关闭

## 5. Strategy API V2 数据加载

- [x] 5.1 为 `CNStock` 日线增加本地 PostgreSQL 加载路径，保持其他市场和非支持周期的原路径不变
- [x] 5.2 将 A 股交易日以 `Asia/Shanghai` 会话日期加载到 pandas/策略事件，增加防 UTC 日期漂移测试
- [x] 5.3 在策略执行前分别校验用户区间、预热区间、质量状态和复权因子覆盖
- [x] 5.4 定义并实现稳定的结构化覆盖错误，包含标的、缺失/异常区间、覆盖水位及建议动作
- [x] 5.5 排除尚未收盘的当日日线，并在截断或拒绝响应中返回明确可用截止日
- [x] 5.6 在回测存储和 API 响应中记录数据供应商、内容版本、覆盖摘要、复权/因子版本和数据截止时间
- [x] 5.7 验证本地缺数时不会调用远程降级链，也不会执行部分多标的组合回测

## 6. A 股撮合与费用规则

- [x] 6.1 在通用撮合器中引入默认兼容的 `MarketExecutionPolicy` 边界，并为现有市场增加回归测试
- [x] 6.2 实现普通 A 股仅做多和禁止负目标仓位规则
- [x] 6.3 实现按交易日维护的 T+1 可卖数量及跨交易日结算
- [x] 6.4 实现 100 股买入单位、非清仓卖出约束和合法零股清仓
- [x] 6.5 实现停牌、无有效 OHLC 和无成交能力时的延期/拒绝逻辑
- [x] 6.6 实现按日期、交易所和证券分类解析涨跌停规则，分类不可靠时失败关闭
- [x] 6.7 实现禁止涨停买入/跌停卖出的日线撮合判断及稳定原因码
- [x] 6.8 实现按生效日期配置的佣金、最低佣金、卖出印花税和过户费分项计算
- [x] 6.9 将分段费用纳入最大可买数量和现金校验，并在成交、订单账本和绩效结果中保存费用明细
- [x] 6.10 在回测结果中发布市场规则版本、T+1、交易单位、涨跌停、滑点和费用假设摘要
- [x] 6.11 为 T+1、零股、涨跌停、停牌、最低佣金、印花税和现金边界增加表驱动测试

## 7. 管理员 API 与审计

- [x] 7.1 增加管理员市场历史 blueprint/路由，并复用现有认证和角色授权边界
- [x] 7.2 实现定向同步创建、批次列表/详情、失败标的重试和受控取消端点
- [x] 7.3 实现供应商节点健康和当前选中节点只读端点，限制主动探测频率
- [x] 7.4 实现按标的查询覆盖、缺口、复权状态、质量问题和最近同步的端点
- [x] 7.5 实现磁盘保护状态端点，返回配置阈值、实时空间、阻断原因和人工操作提示
- [x] 7.6 记录同步创建、取消、重试和修复的操作者、范围与结果，并验证日志不包含秘密
- [x] 7.7 为普通用户拒绝、管理员成功、重复任务、无效范围和磁盘保护编写 API 测试

## 8. Web 前端

- [x] 8.1 在 `/home/quantadinger/QuantDinger-Vue` 增加市场历史管理员 API 客户端和类型/响应适配
- [x] 8.2 在设置页实现供应商健康、节点状态、磁盘保护和总体覆盖概览
- [x] 8.3 实现同步批次列表、进度、错误、定向同步与失败重试交互，并按管理员权限控制操作
- [x] 8.4 实现单标的覆盖、缺口、质量问题和复权状态详情
- [x] 8.5 在回测中心展示结构化覆盖错误、数据来源/版本和 A 股执行假设
- [x] 8.6 对旧后端或功能未启用响应实现明确降级提示，不伪造健康/完整状态
- [x] 8.7 增加前端聚焦测试，并只在资源检查通过后串行构建本次修改的 Web 服务

## 9. 验证与渐进发布

- [x] 9.1 使用少量沪市、深市、ST/非 ST 和含公司行动的权威样本对账原始数据与复权结果
- [x] 9.2 运行数据库、供应商、质量、覆盖、Strategy API V2 和撮合规则的聚焦后端测试，禁止并行 pytest
- [x] 9.3 运行版本、mojibake、Python 语法及 Compose 配置的适用静态检查
- [x] 9.4 若 Agent Gateway 回测响应契约受到影响，更新英文 `docs/agent/agent-openapi.json` 并运行相关 `test_agent_*` 测试
- [x] 9.5 在资源检查满足约束后只构建并更新发生变化的 backend，再单独构建和更新 frontend；不构建 Mobile/H5
- [x] 9.6 在功能开关关闭状态部署迁移和只读状态，验证无现有市场回归
- [x] 9.7 仅对明确批准的首批标的执行串行回填，监控失败率、内存、交换空间、数据库和根磁盘增长
- [x] 9.8 启用本地 A 股回测与市场规则，保存基准回测结果并验证来源/假设可追溯
- [x] 9.9 确认首批系统标的池、默认费用口径和证券分类历史来源后，再配置日常增量同步
