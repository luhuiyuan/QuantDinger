## 1. 后端快照与规范化基础

- [x] 1.1 为沪深 A 股规范标识、交易所推导和快照字段标准化补充单元测试
- [x] 1.2 实现全市场快照适配器，过滤非沪深证券并返回统一报价、行情时间和来源字段
- [x] 1.3 实现 Redis 新鲜/陈旧双层缓存、并发刷新合并、超时与结构化降级
- [x] 1.4 为快照成功、单行缺失、上游失败复用陈旧缓存和完全不可用场景补充测试

## 2. 市场概览能力

- [x] 2.1 实现上证指数、深证成指和创业板指的受控行情获取与部分失败表达
- [x] 2.2 基于同一市场快照计算成交额、涨跌家数及覆盖数量
- [x] 2.3 复用 A 股价格限制规则计算涨停、跌停和无法分类数量
- [x] 2.4 新增受登录保护的 `GET /api/market/cn/overview` 并补充认证、完整结果、部分结果和陈旧结果测试

## 3. 股票目录与用户状态

- [x] 3.1 实现 `qd_market_symbols` 沪深 A 股目录查询及代码/名称搜索、交易所筛选和稳定排序
- [x] 3.2 在共享快照上实现涨跌状态筛选、受限服务端分页和报价缺失表达
- [x] 3.3 仅为当前页连接当前用户自选状态并验证用户隔离
- [x] 3.4 新增 `GET /api/market/cn/stocks` 并覆盖参数校验、搜索、组合筛选、分页、缺失报价和自选状态测试

## 4. 个股详情、日线与技术指标

- [x] 4.1 实现个股身份和报价详情聚合，拒绝非沪深、停用或不存在标的
- [x] 4.2 实现本地权威日线优先读取及覆盖、复权、数据版本和 `backtestEligible` 元数据
- [x] 4.3 实现展示级日线降级及完全无数据场景，确保降级数据不获得回测资格
- [x] 4.4 扩展技术指标计算以返回 MA、MACD、RSI、KDJ、BOLL、ATR 的参数、状态、截止日期和数据不足原因
- [x] 4.5 以规范标识、复权模式和日线版本缓存指标，并验证日线版本变化会使缓存失效
- [x] 4.6 新增 `GET /api/market/cn/stocks/<symbol>` 与 `/history`，补充正常、本地覆盖、展示降级、数据不足和非法标的测试

## 5. 前端 A 股页面

- [x] 5.1 在 QuantDinger-Vue 新增 A 股 API 客户端、中文和英文国际化文案及错误/新鲜度映射
- [x] 5.2 新增 `/cn-stocks` 菜单与页面，实现核心指数、市场统计、数据来源和陈旧/不可用状态展示
- [x] 5.3 实现股票列表搜索、交易所和涨跌筛选、服务端分页、近实时可见轮询及手动刷新
- [x] 5.4 实现列表 A 股红涨绿跌语义、非颜色状态表达和加入/移出自选操作
- [x] 5.5 新增隐藏路由 `/cn-stocks/:symbol` 及个股报价摘要、来源和错误状态展示
- [x] 5.6 为详情页封装轻量日 K 线/成交量图并展示 MA、MACD、RSI、KDJ、BOLL、ATR 指标卡和数据不足原因
- [x] 5.7 实现详情自选状态同步及携带 `CNStock` 和规范股票标识跳转回测中心但不自动运行
- [x] 5.8 补充前端关键逻辑测试，覆盖查询参数、轮询清理、颜色/符号、自选同步、降级来源和回测预填

## 6. 验证、文档与串行部署

- [x] 6.1 运行 OpenSpec 校验、版本检查、乱码检查、后端语法检查和本变更相关后端测试
- [x] 6.2 更新当前环境 Docker 部署/验收文档，说明 A 股页面入口、数据时效、降级语义及管理员定向同步边界
- [x] 6.3 检查 `free -h`、`swapon --show` 和 `df -h /`，按单任务限制构建并部署 backend
- [x] 6.4 验证后端健康和新增 API 后，再次检查资源并按单任务限制构建部署 frontend，禁止与后端构建并发
- [x] 6.5 在部署环境按概览、列表、搜索筛选分页、个股详情、自选、回测预填和陈旧/缺失数据场景完成验收并记录结果
- [x] 6.6 根据代码审查修复逐页报价的部分数据契约、涨跌筛选完整性标记、详情双重上游失败的结构化 `503`、超时 Future 解除复用及前端 `partial` 新鲜度展示，并将实施决策追记到 OpenSpec
