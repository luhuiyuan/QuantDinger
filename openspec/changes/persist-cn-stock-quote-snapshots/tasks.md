## 1. 数据模型与仓储

- [x] 1.1 新增幂等 PostgreSQL migration，创建 `qd_cn_stock_quote_snapshots`、`qd_cn_stock_quote_refresh_runs`、唯一约束及排序查询所需索引
- [x] 1.2 实现最新行情仓储，支持规范化校验、受限批量 UPSERT、失败不覆盖旧值和逐批短事务提交
- [x] 1.3 实现刷新运行记录仓储，保存运行状态、来源、计划/成功/失败/未返回数量、时间和限长安全错误摘要
- [x] 1.4 补充 migration 与仓储测试，覆盖首次创建、重复执行、同标的更新、无效报价拒绝、部分批次提交和旧值保留

## 2. 后台行情刷新服务

- [x] 2.1 抽取完整市场快照覆盖判定及批量持久化服务，保留配置超时并返回结构化刷新结果
- [x] 2.2 实现主源失败后的腾讯目录批量降级，按稳定顺序每批最多 100 只、默认串行、复用限流重试且不退化为逐股票请求
- [x] 2.3 实现 Redis 所有权锁、锁 TTL、运行中检查和重复任务跳过，确保同一时刻最多一个有效刷新
- [x] 2.4 实现交易日、上午/下午会话、午休、非交易日和收盘最终刷新判定，并保证每日最终刷新最多一次
- [x] 2.5 为完整主源成功、主源超时、腾讯全成功、腾讯部分失败、锁冲突、Worker 异常恢复和非交易时段跳过补充聚焦测试

## 3. Celery 调度与运行可观测性

- [x] 3.1 新增 Celery 行情快照任务和每 5 分钟 Beat 配置，任务内部再次执行交易会话及幂等检查
- [x] 3.2 确认 Compose 中 Celery Worker/Beat 使用包含新任务的 backend 镜像和正确队列，避免新增并行 Worker 或无界任务扇出
- [x] 3.3 新增最近刷新状态与覆盖摘要服务/API，向普通用户隐藏敏感错误详情并保留管理员诊断信息
- [x] 3.4 补充任务注册、重复 Beat、部分成功计数、错误脱敏和状态 API 权限测试

## 4. 数据库行情列表查询

- [x] 4.1 将 A 股目录查询改为 `qd_market_symbols` 左连接最新行情快照，保留无报价证券和当前用户自选状态
- [x] 4.2 实现代码、名称、涨跌幅、成交量、成交额、行情时间白名单排序，校验方向并使用稳定代码次排序与 `NULLS LAST`
- [x] 4.3 将涨跌状态、交易所、关键词筛选和总数统计放入同一数据库查询，确保分页总数代表完整数据库快照结果
- [x] 4.4 返回目录数、覆盖数、最新/最旧 `quote_time`、来源、最近任务状态及基于交易会话的 `fresh/stale/closed/unavailable`
- [x] 4.5 保留数据库空表或严重故障时的现有逐页腾讯降级，并继续明确 `partial=true`、`changeStateComplete=false`
- [x] 4.6 补充 API 与查询测试，覆盖各排序字段、组合筛选、稳定分页、NULL 行、陈旧/收盘状态、SQL 白名单和灾难降级

## 5. 前端排序与行情状态

- [x] 5.1 扩展 A 股 API 客户端和查询状态，支持排序字段、排序方向以及数据库覆盖和刷新任务摘要
- [x] 5.2 为股票列表增加可访问的涨跌幅、成交量、成交额排序交互，并将排序条件同步到 URL
- [x] 5.3 展示数据库行情来源、真实 `quote_time`、覆盖率、最近任务状态及 `fresh/stale/closed/unavailable` 中文和英文文案
- [x] 5.4 在部分成功、陈旧、数据库空表逐页降级时展示非误导性警告，同时保持搜索、筛选、分页、自选和详情跳转可用
- [x] 5.5 补充前端逻辑与组件契约测试，覆盖排序参数、URL 恢复、红涨绿跌、覆盖提示、收盘状态和灾难降级
- [x] 5.6 移除 A 股列表和详情的前端定时轮询，使概览和详情默认读取 PostgreSQL，并增加禁止 `setInterval` 及请求链路网络刷新的回归测试
- [x] 5.7 在后台行情批次后同步生成 Redis 市场概览缓存，使概览 API 缓存优先并仅在缓存丢失时从 PostgreSQL重建

## 6. 验证、部署与交易时段验收

- [x] 6.1 运行 OpenSpec 严格校验、migration/仓储/刷新/API 聚焦测试、Python 语法检查和前端单元测试
- [x] 6.2 在执行 migration 和 Docker 构建前检查内存、Swap、根磁盘及预期表/索引空间；不足时停止并请求处理，不自动清理项目或共享缓存
- [x] 6.3 手工执行一次刷新，核对目录数、覆盖率、腾讯批次数、部分失败保留旧值和常用排序查询执行计划
- [x] 6.4 按低内存规则串行构建部署 backend，并依次更新 Celery Worker、Celery Beat，确认只有一个有效刷新任务
- [x] 6.5 串行构建部署 frontend，验收数据库排序筛选分页、行情时间、新鲜度、覆盖率、部分成功和个股详情兼容性
- [ ] 6.6 观察至少一个交易时段的 5 分钟主动刷新、午休跳过和收盘最终刷新，记录运行标识与计数后完成代码审查
