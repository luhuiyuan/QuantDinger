## 1. 持久化模型与配置

- [x] 1.1 新增幂等 PostgreSQL 迁移，创建外部数据请求日志、缓存统计和清理运行状态表，并为时间倒序、提供方/结果筛选和保留删除建立索引。
- [x] 1.2 新增日志结果枚举、脱敏/截断摘要规则和 `ExternalDataRequestLogService` 的仓储接口；确保 API Key、认证头、Cookie、完整查询串、响应体和完整堆栈不能持久化。
- [x] 1.3 在后端设置 schema、`env.example` 与配置加载中增加日志启用开关、成功/异常保留天数、批删除大小和自动清理开关，默认成功 30 天、异常 90 天并设置保守下限。
- [x] 1.4 为迁移、仓储查询、脱敏和配置边界添加单元测试。

## 2. 提供方尝试记录与缓存指标

- [x] 2.1 实现逻辑级 `ProviderAttempt` 辅助器，自动记录耗时、调用来源、关联 `request_id`、降级序号、重试次数、HTTP 状态和固定结果分类，并保证日志写入 fail-open 且不递归记录自身失败。
- [x] 2.2 将辅助器接入优先级/降级链路明确的核心 `data_sources` 与 `data_providers`，先覆盖行情、K 线、宏观、新闻/日历和市场目录调用；为每个已接入提供方记录接入清单。
- [x] 2.3 在缓存返回路径累计轻量命中/未命中统计，不为缓存命中创建外部请求日志。
- [x] 2.4 为成功、超时、限流、提供方业务错误、网络错误、无效响应、禁用/跳过、降级和日志写入故障添加定向测试。

## 3. 管理 API 与自动清理

- [x] 3.1 新增仅管理员可访问的私有 Human API：近 24 小时概览、分页列表、单条脱敏详情，以及日志设置和最近清理状态。
- [x] 3.2 为 API 实现时间范围、提供方、数据域和结果筛选、默认最新倒序、稳定分页及未启用能力的明确响应；保持既有 `{code, msg, data}` 信封。
- [x] 3.3 在 `maintenance` 任务中实现可重入的过期日志批量删除，并在 Celery Beat 每日调度；保存开始/完成时间、删除数量和失败摘要。
- [x] 3.4 为管理员/非管理员权限、分页与详情脱敏、概览统计、保留期分类、批处理清理和清理失败添加后端测试。
- [x] 3.5 更新 `docs/api/openapi.yaml`，将新增管理端点标记为 `x-visibility: private`，并导出/校验 Human OpenAPI 合约。

## 4. Vue 管理页面与设置

- [x] 4.1 在 `QuantDinger-Vue` 新增管理员专属“数据请求日志”路由、API 客户端和多语言文案；普通用户不得看到路由。
- [x] 4.2 实现近 24 小时概览卡、提供方/数据域/结果/时间范围筛选、分页日志表和单条脱敏详情抽屉；页面不得提供重试、重放或删除操作。
- [x] 4.3 将保留期、自动清理开关和最近清理状态接入现有管理员设置页，并对旧后端显示“能力未启用”的兼容降级提示。
- [x] 4.4 为前端筛选映射、结果标签、权限可见性和旧后端降级添加定向单元测试。

## 5. 集成验证与部署准备

- [x] 5.1 运行后端迁移、外部请求日志、管理员 API、维护任务和 OpenAPI 的定向测试；确认日志写入失败不会影响原始数据请求。
- [x] 5.2 运行 Vue 定向单元测试；在已有依赖缓存和资源余量允许时再执行单并发前端构建验证。
- [x] 5.3 使用管理员账户完成手工验收：真实提供方调用、降级链路、缓存命中率、权限拒绝、脱敏详情、设置生效和每日清理状态。
- [x] 5.4 按低内存部署流程顺序构建并滚动更新变更服务：先后端/迁移，再前端；构建前检查内存、Swap 和磁盘空间。
